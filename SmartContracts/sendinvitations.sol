pragma solidity 0.4.21;

//import "./setparameters.sol; 
//import "./safemath.sol";


contract SendInvitations {

	struct Guest{
		uint32 guestId;
		uint32 personality;
	}
	
	Guest[] public guests;
	
	//this maps will be needs in Party
	//mapping (uint => address) guestVIPbyPlayer;
	//mapping (address => uint) playerGuestCount;
	
	function ourGuests (uint _quantity) private {
	
	//parameters to combine
	genA=
	
	//generate personality by func() times by quantity
	uint counter = _quantity;
    for (uint i = 0; i < _quantity; i++) {
	
		block.number
      if (zombieToOwner[i] == _owner) {
        result[counter] = i;
        counter++;
		
      }
    }
	
	
	}


}
